# Copy this file to .env and fill in your real values (never commit .env itself)

ADMIN_KEY=change-this-secret-key

# Gmail address you want emails to be sent FROM
EMAIL_USER=youraddress@gmail.com

# Gmail App Password (NOT your normal Gmail password) — see README for how to get one
EMAIL_APP_PASSWORD=your-16-character-app-password
